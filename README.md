this is .net 8 app , make sure .net 8 sdk is installed 
then open console and run update database to add migrations 
then run the app 
click from swagger on Order method and send the paramters of schoolid ,orderlines and email ,
you can used default values
